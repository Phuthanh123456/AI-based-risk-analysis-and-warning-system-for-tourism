# Runtime dependencies for the FastAPI backend (src/api/app.py).
# For the offline data pipeline (crawl/clean/train PhoBERT), also install
# requirements-train.txt: pip install -r requirements.txt -r requirements-train.txt

requests==2.32.3
PyYAML==6.0.2
scikit-learn==1.5.1
pandas==2.2.2
numpy
fastapi==0.115.0
uvicorn[standard]==0.30.6
geopy
joblib
xgboost
python-dotenv==1.0.1

# Auth (JWT + password hashing)
PyJWT==2.9.0
bcrypt==4.2.0
email-validator==2.2.0

# Web Push (VAPID)
pywebpush==2.0.3

# Testing
pytest==8.3.3
httpx==0.27.2
pytest-asyncio==0.24.0
